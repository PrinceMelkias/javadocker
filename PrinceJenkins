pipeline {
    agent any

    stages {
        stage('Install to Maven') {
            steps {
             sh 'sudo dnf install maven -y'
            }
        }
        stage('Clean package') {
            steps {
                sh 'sudo mvn clean package'
            }
        }
        stage('Artifacts') {
            steps {
                archiveArtifacts 'target/*.jar'
            }
        }
    }
}
